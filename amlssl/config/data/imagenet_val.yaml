imagenet_val:
  name: ImageNet
  args:
    split: valid
    scale: 1
    channels:
      - 0
      - 1
      - 2

  # training data loader configuration
  loader:
    num_workers: 32
    batch_size: 32
    pin_memory: True
    drop_last: False
    shuffle: False

  output_dir: null

