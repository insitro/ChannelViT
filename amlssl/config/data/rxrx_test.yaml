rxrx_test:
  name: RxRx1
  args:
    split: test

    channels:
      - 0
      - 1
      - 2
    scale: 1

  # training data loader configuration
  loader:
    num_workers: 32
    batch_size: 32
    pin_memory: True
    drop_last: False
    shuffle: True

  output_dir: null

